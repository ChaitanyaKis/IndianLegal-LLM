"""IndianLegal-LLM: an open-source, citation-grounded LLM framework for Indian law.

This package is a *walking skeleton*: every component is defined by an abstract
interface (ABC) and ships with a pure-stdlib stub implementation. The full pipeline
runs end-to-end with zero external dependencies. Real implementations replace the
stubs later, behind the same interfaces.

See CLAUDE.md at the repo root for the LOCKED project constraints.
"""

__version__ = "0.1.0"
__license__ = "MIT"

__all__ = ["__version__", "__license__"]
